# chishiki-quiz 共通引き継ぎ

> **Codex・Claude・Gemini共通の作業記録**。作業を始める前に必ず読み、設計・実装・デプロイ・未解決事項に変更があれば、同じ作業コミットでこの文書も更新する。秘密情報（パスワード、Cookie、APIキー）は記録しない。

## 現在のスナップショット（2026-07-28）

- **地図記号**: 33記号を国土地理院の公式一覧に照らして描き直し済み。`dfe6829` でコミット・デプロイ済みで、SVG読み込みは `?v=shinyojurin-scale-v1`。細部の調整履歴は更新履歴のCodexの項を見ること。

- **最新コミット**: 歴史を時代ごとの12クイズに分割（`main` へpush済み、Cloudflare Pagesの自動デプロイ対象）
- **トップ画面**: 教科から始める導線と、教科 → 分野 → 個別クイズのネスト型「習熟度一覧」を実装済み。個別項目はコンパクトなタイルで色を確認できる。
- **習熟度の色**: 8段階（グレー〜赤）＋文言。色の意味は `genres/` の「色の見方」コラムにのみ表示する。
- **先生用メニュー**: 統計・履歴 / 問題一覧 / 問題の登録 の3つ。いずれも生徒からは見えない。
- **コンテンツ**: 本番は `quizzes` 32件 / `items` 1012問。**1行=1問**なので items の件数がそのまま問題数。現役は30クイズ918問。
  - 社会17クイズ678問。分野は地理と歴史。
    - 地理5クイズ290問（日本地理107・世界遺産78・都道府県(地図)47・地図記号33・地形25）。
    - 歴史12クイズ388問（幕末・明治73・江戸後期42・大正昭和戦前38・室町戦国37・鎌倉35・江戸前期30・平安29・現代29・飛鳥24・原始19・奈良16・安土桃山16）。
  - 理科13クイズ240問、分野はすべて生物（環境22・生物の分類20・消化20・呼吸循環20・刺激と反応20・光合成19・遺伝19・花と発生18・排出18・生態系18・生殖17・進化16・細胞13）。
  - 旧地図クリック式2本94問は `is_archived=1`。
- **未解決／次の拡張**: 理科の物理・化学・地学、および公民が未着手（生物は完了）。クイズの追加は `data/quizzes.csv` に1行足すだけで分野分けまで反映される。問題データはCSVに一本化済み（8章参照）。

## 共通運用ルール

1. **開始時**: この文書と `git status` を確認する。
2. **変更時**: 実装内容、確認結果、デプロイの有無、次の作業をこの「現在のスナップショット」または更新履歴へ追記する。
3. **コミット時**: コード変更とこの文書の更新を同じコミットに含める。関係ない未追跡ファイルは追加しない。
4. **競合時**: 他のエージェントの更新を消さず、最新内容を取り込んで追記する。

## 更新履歴

### 2026-07-28 — Claude（15回目）

- **歴史を時代ごとの12クイズに分けた**。`migrations/0016_split_rekishi_by_era.sql` で**本番適用済み**。
  - 388問が1クイズに入っていて、習熟度タイルがほとんど動かなかった。1段位上がるのに数十ラウンドかかる計算になる。
  - 原始 / 飛鳥 / 奈良 / 平安 / 鎌倉 / 室町・戦国 / 安土桃山 / 江戸前期 / 江戸後期 / 幕末・明治 / 大正・昭和(戦前) / 現代。1クイズあたり16〜73問になった。
  - **割り当ては事物(`item_key` の語幹)単位で行う**。`-year` / `-person` は同じ事物の別の聞き方なので、必ず一緒に動かすこと。222事物→388行。
  - `items` は `quiz_id` を UPDATE するだけなので `item_id` が変わらず、解答履歴は保たれる(この移行の時点では歴史の履歴は0件だった)。
  - 元の `rekishi` クイズは参照する `rounds` が無いので削除した。`syakai/rekishi/` も消してある。
  - `category` 列（原始/古代/中世/近世/近代/現代）はそのまま残した。より粗いまとまりの記録として使える。
- **中学の歴史における世界史の量を数えた**: 222事物中12(約5%)。中世4(ルネサンス・宗教改革・コロンブス・マゼラン)、近世4(名誉革命・アメリカ独立宣言・フランス革命・産業革命)、近代4(アヘン戦争・南北戦争・辛亥革命・ロシア革命)。第一次/第二次世界大戦・国際連盟・世界恐慌・冷戦・朝鮮戦争は日本の動きと一体なので日本史側に数えた。**単独のクイズにする量ではない**ので、同時代の日本のクイズに混ぜてある。
- 検証: 12クイズすべてを10問完走(全問正解)、自己ベストが保存されること、社会の一覧が17件になること、トップの選択肢が30件・分野が地理/歴史/生物の3つになることをブラウザで確認。ローカルDBは本番と同じ分割前の状態から migration を当て、items 1012件・rounds 2件・attempts 20件が保たれることを確認。JSエラーなし。

> **`git stash` は未追跡ファイルを退避しない**。CSVを分割した直後の状態で stash すると、`data/quizzes.csv` だけ古いものに戻って新しい `items_*.csv` が残り、`generate-seed.js` が「quizzes.csv に無い quiz_id」で落ちる。分割前後を比べたいときは `git show HEAD:<path>` を使うこと。
### 2026-07-28 — Claude（14回目）

- **`rounds` に `role` 列を足した**。`migrations/0015_add_round_role.sql` で**本番適用済み**。
  - それまで先生が動作確認で1ラウンド回すと、それが生徒の習熟度・レベル・連続プレイ日数・自己ベスト・先生用の統計にそのまま入っていた。
  - `NULL` はこの列を足す前の記録＝生徒のものとして扱う。判定は `COALESCE(role,'student') <> 'teacher'` で統一する。
  - 条件式は `functions/_lib/mastery.js` の `studentRounds(alias)` と `joinStudentRounds(a, r)` にまとめてある。**`attempts` や `rounds` を読むクエリを新しく書くときは、必ずこのどちらかを通すこと**（現在9箇所すべてが通っている）。
  - `attempts` 自体には role を持たせていない。`round_id` から辿れるので冗長になるため。
- **トップの「学習を始める」をランダムから選択式に変えた**。
  - 以前は出題中のクイズから等確率で1つ引いて飛ばしていた。理科13クイズを足した時点で、押すと68%の確率で理科に飛ぶようになっていた（社会は6/19）。
  - 押すとその場に全19クイズが開き、本人が選ぶ。並びは習熟度の低い順で、教科と分野と段位の色を出す。
  - 検討して見送った案: 「一定の習熟度まで同じジャンルを続ける」。習熟度%は未出題も分母に入るため問題数に強く依存し、同じ閾値でも細胞のつくり(13問)は2〜3ラウンド、歴史(388問)は60〜80ラウンドかかる。一律の閾値は置けない。
- 検証: 先生で1ラウンド回した後に生徒のXP・レベル・連続日数・総合スコア・習熟度(重み合計65のまま)が一切動かないこと、その後に生徒が回すと動くこと、先生用の統計に先生の分が出ないこと、選択式が19件出て遷移することをブラウザで確認。JSエラーなし。

> **Playwrightでラウンドを最後まで回すときの注意**: 解答後も「決定」ボタンが無効状態で残るので、`button:not([disabled])` で拾うと「次へ」ではなくそちらを掴む。`button:has-text("次へ")` のようにテキストで指名すること。また **`renderResult()` まで進めないとラウンドはPOSTされない**。10問答えただけで止めると `rounds` に1行も残らない。
### 2026-07-27 — Claude（13回目）

- **理科(生物分野)を本番に載せた**。`migrations/0014_add_rika.sql` で**適用済み**。13クイズ240問、`genre=rika` / `section=生物`。既存データには一切触れていない(INSERT OR IGNORE のみ)。
- 出発点は Gemini が作った `drafts/rika_questions_draft.csv`（268問）と `drafts/RIKA_REVIEW_REPORT.md`。268→240問に整理した。
  - **削除28問**: 重複17（クイズ「細胞」だけで7。同じ答えを言い換えただけの問題が多かった）、高校範囲9（窒素循環8・中間型生物1）、問いと答えが噛み合っていないもの2。
  - **問題文の修正7件**: 脂肪の分解物を「脂肪酸とグリセリン」→「脂肪酸とモノグリセリド」（現行教科書の表記）、裸子植物の定義、「下にある地層ほど何時代か」→答えが「古い」で問いに答えていない、など。`は虫類` と `爬虫類` の表記ゆれも統一した。
  - **別解29件**を追加・修正した。
- **レポートの指摘の大半は採らなかった**。理由は下の注記のとおり、正規化で既に通るため。読点区切りの別解（実際に壊れていた）と全角コロン、漢字の読みだけ入れた。
- **`quiz_id` を内容が分かる名前にした**。ドラフトの `rika-shokubutsu-01`〜`-14` という通し番号を `rika-saibou` / `rika-kougousei` のような形に変え、1ファイルにまとまっていた問題を `data/items_<quiz_id>.csv` 13本に分けた。`item_key` は削除で空いた番号を詰め直してある(本番に無いデータなので履歴への影響なし)。
- **ドラフトのクイズ14を廃止した**。窒素循環8問を削ると2問しか残らず1ラウンド10問に足りないため、残った「持続可能性」「環境アセスメント」を環境のクイズへ移した。
- `rika/index.html` を「準備中です」から `syakai/index.html` と同じ一覧に差し替え、`rika/<slug>/index.html` を13枚作った。トップの教科カードと習熟度一覧は元からデータ駆動なので変更不要だった。
- 検証: ローカルDBを作り直して13クイズすべてを10問完走、別解17パターンの判定、トップに理科が出ること、先生用の問題一覧に13タブが出ることをブラウザで確認。JSエラーなし。

> **別解を書く前に `assets/quiz.js` の `normalizeAnswer` を読むこと**。カタカナ→ひらがな・全角→半角・大文字小文字・長音や中黒の除去は**自動で効く**。`くろろふぃる`・`でんぷん`・`dna`・`Ｆ１`・`３対１` などは accept に書く必要がない。今回のレビューはこれを知らずに不要な別解を多数勧めていた。効かないのは**漢字の読み**と**全角コロン**。

> **ひらがな別解の基準**: 常用漢字外の字を含む語（だ液・すい液・血しょう・ろ過・柔毛）と、教科書がかな書きしている語（ぼうこう・末しょう神経・は虫類）だけに入れる。網膜・鼓膜のような普通に書ける語には入れない。全部に読みを足すと「漢字で書けなくてよい」ことになるため。

> **accept の区切りは `|` だけ**（`scripts/generate-seed.js` の仕様）。ドラフトには読点区切りの行があり、`F₁、第一世代、子の代` が丸ごと1つの別解として登録されるところだった。どれも正解にならない。
### 2026-07-27 — Codex（`dfe6829` でコミット・デプロイ済み）

- `migrations/0013_rename_minato_to_kowan.sql` は**本番適用済み**（`minato` の answer が「港湾」であることを確認）。
- 地図記号 `minato` の正解・表示名を「重要港」から**「港湾」**へ変更。`港` と旧表記の `重要港` は別解として受け付ける。
- 正本CSVを更新し、既存DB向けの `migrations/0013_rename_minato_to_kowan.sql` を追加。SVGの内部キー `minato` は互換性のため維持する。
- 港湾のSVGは既存の錨形を維持。中央の縦線を下の曲線へ接続し、下側の曲線の先端からそのまま左右の矢印へ続く形に修正した。地図記号クイズ・先生用一覧・ローカル確認ページは `?v=kowan-anchor-v2` で新SVGを読む。
- 港湾の下側の曲線は既存の形を維持。矢じりの根元を曲線の両端へ直接密着させ、余計な角のない小さな外上向きの塗りつぶし矢じりにした。

### 2026-07-27 — Codex（同じく `dfe6829` に含まれる）

- 初回の比較で `地図記号一覧.png`（現行出力）を視覚的な補助に使い、既存SVGの誤りを引きずった。**以後、このPNGは正誤判断に使わない**。国土地理院の個別公式記号だけを見て、既存SVGの形を参照せず描き直す。
- 国土地理院の[地図記号一覧](https://www.gsi.go.jp/kohokocho/map-sign-tizukigou-2022-itiran.html)を正解資料として、税務署・消防署・発電所・荒地の4記号を試行修正した。
- `assets/chizu-kigou-symbols.js` のみを変更。税務署には上下の短い縦線、消防署にはU字と中央の柄を反映した。荒地は「中央が長い3本線」の単位記号を3つ三角配置にした。発電所は工場の横線を延長し、左端を下・右端を上へ同じ太さで折る形にした。
- 公式の表記「発電所等・送電線」は、発電所と送電線を別記号として併記したもの。現在の問題データ「発電所」は変更していない。
- 形の微調整（スナップショットから移動）: 塗りつぶしの二等辺三角形を置き、三角形の重心を曲線端へ一致、二等分線を曲線端の接線と平行にした。頂角の向きは接線に対して180度反転済みで、底辺を広げて頂角を大きくした。上部の丸は半径を13から11へ縮小した。高等学校記号は横棒を外円に接する長さへ延長し、内部線を太く、斜線の下端も外円まで延長したうえで、斜線同士の開きを少し狭め、上端を横棒と重ねて接続した後、横棒と斜線で囲まれる白い逆三角形を広げた。小・中学校記号は斜線の上端を横棒内へ少し下げ、butt端にして上への角のはみ出しをなくしつつ、余白が出ない位置へ再調整した。針葉樹林は左右の斜線を一本の連続パスにして頂点の余白をなくし、広葉樹林の外接サイズに揃えた後、頂角をさらに細くし、線幅を6.5へ下げ、横線を左へ2移動した後に右へ1戻した。広葉樹林も線幅を6.5に揃え、下の横線を左右からさらに2ずつ短くした。温泉記号は3本の湯気線の横間隔を少し詰め、老人ホームは中央の杖を正しい向き（左は短い持ち手、右が床まで伸びる杖）に修正し、さらに建物中央へ2だけ左寄せした。SVG読み込みは `?v=forest-lines-v3` に更新済み。Edgeで描画確認済み。
- さらに追加の微調整（同上）: さらに小さく調整し、横線を右へ合計3移動した後、全体を約5%縮小した。桑畑は木を1つだけにし、中央へ下げて大きくした。SVG読み込みは `?v=shinyojurin-scale-v1`。
- `review/map-symbol-pilot.html` を33記号すべてのローカル確認ページに拡張した。サーバーを起動せず直接開ける。
- 国土地理院の個別公式画像で明確な差を確認した灯台（中心を塗りつぶしから輪へ）、裁判所（三角と柄）、病院（盾形）、記念碑（丸い上部の碑）、博物館（切妻屋根と柱）を描き直した。
- 続けて、図書館（斜めに開く本）、針葉樹林（A字と横棒）、保健所（円に触れない十字）を公式形状へ描き直した。
- 追加調整: 学校・高等学校の上点を垂直線に統一。郵便局は文字フォントを使わず、円に接する同一太さの線で〒を描画。市役所の内円は細線化し、博物館のひさしを短縮した。
- 警察署・広葉樹林・桑畑・田・畑・老人ホーム・図書館・温泉・果樹園の線端と角を角形に統一し、不要なはみ出しと隙間を調整した。茶畑も「3粒の単位記号」を3つ三角配置に変更した。
- 再調整: 郵便局の長い横線を下げ、学校の斜線を左右対称にした。高等学校の横線端は円との接続で角が出ないようにした。図書館は以前の開いた本の形へ戻した。
- 記念碑の右側だけに台座の出を追加。病院の五角形を横長にし、十字を外枠から離した。老人ホームは屋根から出る壁線をなくして、杖を左右とも床へ届く細線に描き直した。針葉樹林は公式の細い三角形と下辺に戻し、広葉樹林は幹を樹冠の接点まで伸ばした。
- 再々調整: 学校・高等学校の上線を横棒まで接続。郵便局は上下の横・縦線を円の接点まで伸ばした。記念碑には下辺を追加し、針葉樹林の下辺は右半分だけにした。桑畑は単位記号を3つ三角配置にした。
- 温泉は湯船を横に広げ、湯気を少し大きく波立たせた。老人ホームは、屋根を壁より張り出させつつ壁の端を屋根より上へ出さず、杖の左・右とも床線へ接続した。
- 再々々調整: 針葉樹林は細い三角形の**最下部の横棒**を右へ大きくずらし、右の斜線から明確に出す形にした。ほかの線端は余計に張り出さないよう butt 指定にした。温泉の湯船は約240度の円弧へ変更。桑畑の3単位記号はさらに小さくした。
- 再々々々調整: 温泉は正面からの円弧ではなく、奥行きを表す横長の楕円弧（約240度）に変更。高等学校は上線を内側の円へ接続し、横線端は円の内縁で止めて角が出ないよう再調整した。
- 再々々々々調整: 温泉の湯気3本を下へ移動。高等学校の上線は縦線を維持し、横線端だけを外円の前面描画で隠す方式に変更した。

#### 残り29記号の進捗（5件ずつ）

- [x] 1/6: 神社、寺院、郵便局、小・中学校、高等学校（国土地理院の個別画像で確認。小・中学校・高等学校の線端を角形へ修正）
- [x] 2/6: 市役所、町村役場、交番、警察署、工場（工場は発電所の中心に用いた歯車部分と同じで正しい）
- [x] 3/6: 図書館、博物館、老人ホーム、灯台、重要港（国土地理院の個別画像で確認し、図書館・老人ホーム・灯台・重要港を描き直し）
- [x] 4/6: 温泉、三角点、裁判所、保健所、病院（国土地理院の個別画像で確認。温泉の湯気と湯船、三角点の鋭角と中心点を調整）
- [x] 5/6: 記念碑、墓地、果樹園、田、畑（国土地理院の個別画像で確認。記念碑の幅、墓地の線幅と横棒、果樹園の円と幹を調整）
- [x] 6/6: 茶畑、桑畑、広葉樹林、針葉樹林（茶畑・広葉樹林・針葉樹林は国土地理院の個別画像で確認し調整。桑畑は現行一覧から外れた旧記号のため、国土地理院を出典とする旧資料の形を維持）

### 2026-07-27 — Claude（12回目）

- **地図記号を22問 → 33問にし、形の誤りを直した**。`migrations/0012_chizu_kigou_fix.sql` で**本番適用済み**。
  - 所有者から `家庭教師用/地図記号参照.pdf`(国土地理院の記号一覧)の提供を受け、実物と突き合わせて描き直した。**記憶で描くと誤りが多い。今後も必ずこのPDFと照合すること**。
  - 直したもの: 警察署(バツが円に接する)・交番(縦長)・郵便局と高等学校(字を大きく)・神社(下の貫を柱の内側に)・消防署(先が垂直に立つ)・老人ホーム(杖が床につく)・灯台・重要港(爪が外へ張り出す)・桑畑(幹の下が右に折れる)・広葉樹林(円＋右の枝)・針葉樹林・茶畑(小さい実を3つ)・税務署(玉を串が貫く)・保健所(丸＋十字)。
  - **「文」は文字ではなく直線で描く**。書体によって払いが曲線になり、実物と違って見えるため。
  - **田・畑・茶畑・荒地は単位記号を3つ三角形に並べる**。実際の地形図と同じ見え方にするため。
  - 追加した11問: 裁判所・税務署・保健所・病院・記念碑・墓地・果樹園・田・畑・荒地・発電所。
    - PDFに無い**病院と墓地は出典を調べた**。病院は旧陸軍衛生隊の五角形＋赤十字、墓地は墓を横から見た形(逆T字)。
  - 港は「重要港」に統一(答えは重要港、港・港湾も正解)。
- **PDFの確認方法**: この環境には poppler が無く `Read` でPDFを開けない。`pdfjs-dist` と `@napi-rs/canvas` をscratchpadに入れてPNGに変換し、行ごとに切り出して拡大すると細部まで読める。
- **作業中の失敗**: `[^]*?` を使った正規表現でSVG定義を置換しようとして、ファイルの大部分を消した。`git checkout` で復元。**複数ブロックにまたがる置換は正規表現でやらず、ファイル全体を書き直すこと**。
- 検証: 33記号すべての描画を画像で目視確認し、PDFと照合。CSVとSVG定義のitem_key対応も突き合わせ済み(欠け・余りなし)。
### 2026-07-27 — Claude（11回目）

- **データモデルを「1行=1問」に変更した**。`migrations/0011_one_row_one_question.sql` で**本番適用済み**。これが現在の最重要ルール。
  - それまで `label` の意味がクイズごとに違っていた。日本地理だけ問題文で、地形・世界遺産・歴史・地図記号は「名称」が入っていて、問題文は各ページのJSが `hint` などから組み立てていた。
  - **`items.label` には必ず問題文が入る**。生徒に表示される文字列そのもの。`answer` が正解、`extra_json.accept` が別解。
  - 1つの事物から複数の問い方を作っていたクイズは行を分けた。歴史 222→388（説明→語句 / 出来事→年代 / 業績→人物）、世界遺産 26→78（説明→名称 / 名称→所在県 / 名称→種別）。増えた行の `item_key` は `<元のキー>-year` のように接尾辞を付けている。
  - 既存543件は `item_key` を据え置いて `UPDATE` したので `item_id` が変わらず、解答履歴は保たれている。
  - **習熟度の単位が「事物」から「問題」に変わった**。「十七条の憲法」の語句・年代・人物がそれぞれ独立して5段階管理される。名前は言えるが年号が出てこない、を個別に追える。
- **出題文の生成コードが無くなった**。`assets/questions/*.js` は図を作る関数だけになり、文字だけのクイズは登録すら不要（`ChishikiQuestions.typesFor(id)` が `label` をそのまま問題文にする）。出題ページは30行程度。
- **先生用の問題一覧はビューが1つに戻った**。DBの中身がそのまま出題内容なので、分けて表示する必要が無くなったため。図を使うクイズ（地図記号・都道府県の地図）だけ、問題文が同一で見分けが付かないので絵の列を足している。
- **問題の登録フォームを新しい形に合わせた**。「ヒント文」を廃止し、「問題文」「正解」「別解」を入力する。
- 検証: 6クイズすべてが図つきで10問完走すること、別解（1192年 / 十七条憲法 / 複数県）が通ること、一覧が388問を表示することをブラウザで確認。本番は items 761件・履歴（rounds 1 / attempts 10）保持。JSエラーなし。

> **作業時の注意**: `data/` のCSVには改行を含むセル（問題文）が大量にある。行単位で `split` するスクリプトは必ず壊す。RFC4180パーサを通すこと。同じ事故をこの作業中にも起こした。
### 2026-07-27 — Claude（10回目）

- **別解の書き方を `accept` 列に統一した**。`migrations/0010_unify_accept.sql` で**本番適用済み**。
  - 「飛騨山脈(北アルプス)」のように `answer` の括弧で別名を併記していた6項目(飛騨/木曽/赤石山脈・黒潮・親潮・ル・コルビュジエの建築作品)を、`answer=飛騨山脈 / accept=北アルプス` に分けた。`item_key` と `label` も括弧なしに揃えている。
  - 括弧まで打たないと不正解に見えること、別解の表現が括弧と `accept` 列の二本立てだったことが理由。**本番に括弧付きの answer / item_key は残っていない**。
  - 判定側は括弧の分解を残してある。データ側では使わないが、生徒が「飛騨山脈(北アルプス)」と両方書いてきた場合に正解にするため(入力側も分解するようにした)。
- **問題一覧に「DBの中身」ビューを追加**。`items` テーブルの列(item_key / label / answer / accept / category / extra_json)をそのまま並べ、列名と型も見出しに出す。「実際の出題」ビューと切り替えられる。
  - `accept` は `extra_json` の中にあるが、確認しやすいよう独立した列として表示している。
  - 自動で許容される表記(カナ⇄かな・全角半角・接尾辞・長音や中黒)は行ごとに出すと煩いので、ビュー上部に注記としてまとめた。
- 検証: 判定15ケース全通過(括弧の有無・全角括弧・接尾辞・カナ・空入力・誤答)。6クイズすべてで誤答10問が0点になること、DBビューに括弧付きが残っていないことをブラウザで確認。JSエラーなし。
### 2026-07-27 — Claude（9回目）

- **出題定義を各ページから `assets/questions/<quiz_id>.js` に切り出した**（コードのみ、DB変更なし）。
  - きっかけは先生用の問題一覧。「問題文」列に項目名しか出ておらず、実際の出題文（「次の説明にあてはまる地形は？…」）が見えなかった。出題文は各ページのJSが組み立てていたので、一覧側から参照できなかったのが原因。
  - `assets/questions.js` がレジストリ。`ChishikiQuestions.register(quizId, questionTypes)` で登録し、出題ページは `ChishikiQuestions.get(id)`、一覧は `previewsFor(id, item)` で使う。
  - **一覧に「実際の出題」列ができた**。1項目から作られる問題を全パターン（歴史なら語句・年代・人物の3つ）、答えと別解つきで表示する。
  - 別解の表示はエンジンと同じ条件（`q.answer === item.answer`）に揃えてある。年代問題に項目の別解を出すと嘘になるため。
  - `todofuken-chizu` の地図はページの `<template>` にあるので、テンプレートが無い画面では図を省いて出題文だけ返すようにした。
- **新しいクイズを作るときは出題定義もこのディレクトリに置くこと**（7章の手順を更新済み）。ページ内に直接書くと、一覧に出題文が出なくなる。
- 検証: 6クイズすべてが図つきで今まで通り動くこと、一覧に歴史388問・世界遺産78問の出題文が出ること、別解が正しい問題にだけ付くことをブラウザで確認。JSエラーなし。
### 2026-07-27 — Claude（8回目）

- **クイズの分野をDBに移した**。`quizzes` に `section`(分野)と `sort_order`(表示順)を追加。`migrations/0009_quiz_sections.sql` で**本番適用済み**。
  - それまで分野の割り当ては `index.html` の `SECTION_BY_QUIZ` にベタ書きで、クイズを足すたびに手で追記が必要だった。理科のように単元が多い教科を入れると破綻するため解消した。**この定数は廃止**。
  - 画面の階層は 教科(`genre`) → 分野(`section`) → クイズ。分野の並び順は「その分野に属する最初のクイズの `sort_order`」で決まる。値は10刻みにしてあるので、間に入れたいときは中間の値を使う。
  - `/api/quizzes` と `/api/dashboard` は `ORDER BY sort_order, id` で返すようになった。以前は順序が未指定だった。
  - **クイズを追加する手順が「`data/quizzes.csv` に1行足すだけ」になった**(section と sort_order を書けば、トップの分野分けも並び順も自動)。
  - `generate-seed.js` は section 未記入をエラーにする。
- **注意**: 0009 は `ALTER TABLE` を含むため冪等ではない。2回流すと「列が既にある」で失敗する。
- 検証: (A)空DBから schema+seed で構築、(B)旧スキーマのDBに0009を適用、の両方を確認。Bでは既存の履歴と543問が保持され、section未設定が0件であることを確認。トップの習熟度一覧が「社会 → 地理/歴史 → 各クイズ」とDB通りに並ぶこともブラウザで確認。JSエラーなし。

### 2026-07-27 — Claude（7回目）

- **正誤判定を大幅に緩めた**（別エージェントの精査レポート `QUESTION_REVIEW_REPORT.md` への対応）。記述式なのに完全一致だったため、正しく分かっている生徒が不正解になる状態だった。`migrations/0008_add_accept.sql` で**本番適用済み**。
  - `normalizeAnswer()` で吸収するようにしたもの: カタカナ⇄ひらがな（ヤマセ／やませ）、全角英数⇄半角（１１９２／1192）、空白、長音・中黒・カッコ（五・一五事件／五一五事件）。
  - 接尾辞「県府都市区町村」は**双方向**で省略可にした。正解が「青森県」でも「青森」でも、生徒がどちらで打っても通る。レポートの提案コードは正解側からしか削っておらず逆方向が抜けていたので、そこは直して実装した。
  - **CSVに `accept` 列を新設**。項目ごとの別解を `|` 区切りで書ける（例: `リアス式海岸|リアス`）。`generate-seed.js` が `extra_json.accept` に畳み込む（スキーマは変えていない）。28項目に設定済み。
    - エンジン側は、**その項目の答えそのものを問う問題にだけ** `accept` を効かせる。年代や人物を問う問題にまで効かせると別の答えを正解にしてしまうため（`buildRound()` の `q.answer === item.answer` 判定）。
- **レポートが見落としていた実バグを修正**: 世界遺産の「所在都道府県」問題は `prefectures[0]` だけを正解にしていたため、**26問中9問で事実として正しい答えが不正解**になっていた（富士山に「静岡県」など）。全県を `accept` に渡して解決。誤答時の表示も全県を出すようにした。
- 地図記号の「桑畑」「工場」は2013年の図式改訂で廃止済みだったため、ヒントにその旨を注記した（レポートは桑畑のみ指摘していたが、工場も同時に廃止されている）。出題からは外していない。
- `ChishikiQuiz.isAnswerCorrect` / `normalizeAnswer` を公開した。判定仕様をブラウザのコンソールから直接試せる。
- **作業中の失敗と教訓**: CSVを `split(/\r?\n/)` で行分割したところ、改行を含むセル（農水産ランキングの問題文）が壊れた。**`data/` のCSVを加工するスクリプトは必ずRFC4180パーサを通すこと**（`generate-seed.js` の `parseCsv()` を流用する）。
- 検証: 判定ロジックの20ケースを全通過（カナ・全角・接尾辞・カッコ・複数県・空入力・誤答）。6クイズすべてで誤答10問が0点になること（誤判定で正解にならないこと）と、複数県の遺産に末尾の県で答えて正解になることをブラウザで確認。JSエラーなし。
### 2026-07-27 — Claude（6回目）

- **歴史クイズを170問 → 222問に拡充**。`migrations/0007_add_rekishi_genshi.sql` で**本番適用済み**。
  - **原始(新カテゴリ)** — 教科書の最初の単元がまるごと抜けていたので新設。縄文土器・竪穴住居・貝塚・土偶・三内丸山遺跡・弥生土器・稲作・高床倉庫・登呂遺跡・吉野ヶ里遺跡・青銅器・古墳・前方後円墳・大仙古墳・埴輪・大和政権・渡来人 の17問。
  - 古代: 壬申の乱・藤原京・院政・平将門の乱・保元の乱
  - 中世: 二毛作・定期市・馬借・倭寇
  - 近世: 生類憐みの令・田沼意次・大塩平八郎の乱・異国船打払令・新田開発・工場制手工業・五街道・天明のききん
  - 近代(大正〜戦中): 日英同盟・米騒動・政党内閣・大正デモクラシー・普通選挙法・治安維持法・関東大震災・世界恐慌・満州国・国際連盟からの脱退・五一五事件・二二六事件・国家総動員法
  - 現代: 55年体制・四大公害病・東海道新幹線・日中国交正常化・バブル経済
  - 時代別は 原始17・古代39・中世45・近世50・近代53・現代18。年あり105・人物あり61。
- **データ投入スクリプトに検査を追加**（`scratchpad` 側の使い捨てスクリプトだが、次に同じ作業をするときの注意点）: ヒント本文にラテン文字が3字以上続いていたらエラーにする。日本語の文章を大量に書くと英単語が紛れ込む事故が実際に複数回起きたため。
- 検証: 222問にitem_key重複・hint欠けが無いこと、100問連続出題して3パターンすべてが出ること、人名ラベルへの年代問題が0件であることをブラウザで確認。JSエラーなし。
### 2026-07-27 — Claude（5回目）

- **歴史クイズを114問 → 170問に拡充**。`migrations/0006_add_rekishi_seido.sql` で**本番適用済み**。
  - 制度・用語: 班田収授法・口分田・租調庸・防人・荘園・守護と地頭・執権・御恩と奉公・惣・座・土一揆・下剋上・分国法・楽市楽座・兵農分離・五人組・出島・朱印船貿易・株仲間・打ちこわし など
  - 世界史との関連: ルネサンス・宗教改革・コロンブス・名誉革命・アメリカ独立宣言・フランス革命・産業革命・アヘン戦争・南北戦争・辛亥革命・ロシア革命・国際連盟・第二次世界大戦
  - 近代の政策と戦後: 富国強兵・殖産興業・版籍奉還・徴兵令・西南戦争・下関条約・三国干渉・ポーツマス条約・農地改革・財閥解体・冷戦・朝鮮戦争・石油危機
  - 時代別は 古代34・中世41・近世42・近代40・現代13。年あり85・人物あり57。
- **不具合修正**: 人物名がラベルの3項目(平清盛・源頼朝・コロンブス)に `year` が入っており、「『源頼朝』は西暦何年のことか」という不自然な出題が発生しうる状態だった。`year` を外して解消(年の情報はヒント文に残るので学習内容は変わらない)。同じmigrationに `UPDATE` を含めてある。
  - **今後の注意**: `year` は「出来事」を表す項目にだけ付けること。ラベルが人名の項目に付けると年代問題が成立しない。
- **まだ手薄な領域**: 縄文・弥生・古墳時代(三内丸山遺跡・登呂遺跡・稲作の伝来・古墳・埴輪・渡来人)、江戸の三大改革以外の出来事(生類憐みの令・田沼意次・大塩平八郎の乱)、大正デモクラシー(米騒動・普通選挙法・治安維持法)。あと50問程度は足せる。
- 検証: 170問にitem_key重複・hint欠けが無いこと、100問連続出題して人物への年代問題が0件であること、3パターンすべてが出ることをブラウザで確認。JSエラーなし。
### 2026-07-27 — Claude（4回目）

- **歴史クイズを63問 → 114問に拡充**。`migrations/0005_add_rekishi_bunka.sql` で**本番適用済み**。
  - 追加したのは、それまでほぼ空だった**文化史と人物**。各時代の文学・美術・宗教・学問(古事記/源氏物語/鎌倉新仏教/能/水墨画/元禄・化政文化/蘭学/国学 など)と、紫式部・雪舟・千利休・松尾芭蕉・杉田玄白・福沢諭吉・陸奥宗光などの人物。
  - 時代別は 古代26・中世27・近世29・近代25・現代7。年あり69・人物あり52。
  - 年号は 陸奥宗光の領事裁判権撤廃(1894)・小村寿太郎の関税自主権回復(1911)・民撰議院設立の建白書(1874)・大日本沿海輿地全図の完成(1821) を出典にあたって確認した。
- **問題を選ぶ基準**(今後の追加時の参考): ①中学の教科書に必ず出る定番であること ②年号に争いがないこと(鎌倉幕府の成立は1185年説と1192年説があるため、「源頼朝が征夷大将軍に任じられた年」に置き換えている) ③一問一答として自然に問えること。
- **まだ手薄な領域**: 制度・用語(班田収授法・荘園・守護と地頭・惣・座・楽市楽座・五人組)、世界史との関連(ルネサンス・宗教改革・産業革命・フランス革命)、戦後(石油危機・55年体制・バブル経済)。中学歴史だけならあと50〜80問は足せる。
- 検証: 114問にitem_keyの重複とhint欠けが無いこと、3パターンすべてが出題されること、5ラウンド完走をブラウザで確認。JSエラーなし。

### 2026-07-27 — Claude（3回目）

- **歴史クイズ(`rekishi`)を追加**。63項目、`migrations/0004_add_rekishi.sql` で**本番適用済み**。地理以外の分野はこれが最初。
  - 古代〜現代を1クイズにまとめ、`category` で時代(古代・中世・近世・近代・現代)を分ける。
  - 出題は3パターン: 説明→語句 / 出来事→年代 / 業績→人物。1項目から複数の問い方を作るので、`extra_json` に `hint` / `year` / `person` / `achievement` を持たせている(63項目中、年あり58・人物あり27)。
- **共有エンジン(`assets/quiz.js`)に2つ追加**。他クイズにも効くので注意。
  - `questionTypes[].supports(item)` — その項目に出せる問い方だけに絞る。年代を持たない項目に年代問題を出さないため。未定義なら全項目に出せるとみなすので、既存クイズは影響を受けない。
  - **別解の許容** — 正誤判定が完全一致ではなくなった。問題タイプが `accept`(配列)を返せばその表記すべてを正解にする。指定が無い場合も、正解が「黒潮(日本海流)」のように括弧で別名を併記していれば「黒潮」「日本海流」どちらでも正解にする。
    - これにより、**地形クイズの5項目(飛騨山脈・木曽山脈・赤石山脈・黒潮・親潮)が括弧まで正確に打たないと不正解だった問題が解消**した。
    - 歴史の年代問題は `accept` で「1192」「1192年」の両方を受け付ける。
- トップの `SECTION_BY_QUIZ` に `rekishi: "歴史"` を追加。
- **検証**: 3パターンすべてが出題されること、年代が両表記で正解になること、括弧の別解が通ること、既存5クイズ+歴史の計6クイズが10問完走すること、トップに地理/歴史の2分野が出ることをブラウザで確認。JSエラーなし。
### 2026-07-27 — Claude（2回目）

- **先生用に「問題一覧」ページ(`teacher/items/`)を追加**。登録済みの問題を見る場所が無かったため。
  - クイズをタブで切り替え、`#` / 問題文(地図記号は記号の絵) / 正解 / 分類 / 補足 / 習熟度 を表で表示する。アーカイブ済みのクイズも見られる(その旨の注記が出る)。現役クイズが先、アーカイブ済みが後ろの順。
  - 生徒がURLを直接開いてもアクセス拒否になる(`/api/me` の role で判定)。
- **地図記号のSVG定義を `assets/chizu-kigou-symbols.js` に切り出した**。クイズ画面と一覧ページの2箇所から使うため。`ChizuKigou.build(item_key)` が `<svg>` 要素を返し、`ChizuKigou.keys()` で定義済みキーを列挙できる。**記号を足すときはこのファイルとCSVの両方に同じ `item_key` で追加すること**。
- 検証: 生徒でのアクセス拒否、7クイズ分のタブ表示、地図記号22件すべての絵の描画、アーカイブ注記、クイズ本体が壊れていないことをブラウザで確認。JSエラーなし。

### 2026-07-27 — Claude

- **地図記号クイズ(`chizu-kigou`)を追加**。22記号、`migrations/0003_add_chizu_kigou.sql` で**本番適用済み**。
  - 記号はSVGで自前描画し、`item_key` と `id="sym-<item_key>"` を対応させて引く。外部画像を使わないので著作権・リンク切れの心配がない。(当初はページ内の `<template>` に置いていたが、同日中に `assets/chizu-kigou-symbols.js` へ切り出した。下の「2回目」を参照。)
  - 卍 / 文 / 〒 は記号そのものが文字なので `<text>` で描画している。`text-anchor` と `dominant-baseline` はCSSではなく**SVG属性で指定しないと中央に来ない**(一度ハマった)。
  - 線は `currentColor` で描き、色は `.quiz-symbol-visual` 側で決めるためダークモードに追従する。
  - **画像問題を作るときの雛形**でもある。`build(item)` が `{prompt, answer, visual}` を返せば `visual`(DOM要素)が問題文の上に表示される。写真を使う場合は `extra_json` に画像パスを入れて `<img>` を組み立てればよい。
  - **検証**: 22記号すべての描画をスクリーンショットで目視確認。10問1ラウンドを通しでプレイし、7/10の採点・自己ベスト更新・復習ボタン・トップの集計(7/50、Lv3)が想定どおりであることを確認。
- トップの `SECTION_BY_QUIZ` に `chizu-kigou` を追加。**クイズを足したらここも更新すること**(未登録だと「その他」に落ちる)。
- `assets/tier.js` の最下位ランク名の綴りを修正(Hallo → Hello)。

### 2026-07-26 — Claude（2回目）

- **問題データをCSVに一本化**。`scripts/generate-seed.js` のJSハードコードを廃止し、`data/quizzes.csv` + `data/items_<quiz_id>.csv` を正本とするCSV→SQL変換スクリプトに書き換えた。RFC4180準拠パーサ内蔵（npm依存は増やしていない）。生成時に必須列・未知の`quiz_id`・`item_key`重複・`extra_json`のJSON妥当性を検証し、`ファイル名:行番号`付きでエラーにする。
  - 旧CSV4本と `todofuken_chizu_setup.sql` は per-quiz CSV に統合したため削除。
  - **検証**: 生成した `seed.sql` を空のローカルD1に流し、本番エクスポートと突き合わせて quizzes 6件・items 299件が一致することを確認（差分は下記の改行修正9件のみ）。
- **バグ修正**: 農水産ランキング9問の問題文で、改行が実際の改行でなくリテラルの `\n`（2文字）としてDBに入っており、画面に「\n」が見えていた。`migrations/0002_fix_literal_newlines.sql` を追加し**本番適用済み**（9行更新、再実行しても安全）。
- これにより、前回記載した残課題（CSV投入手順が非定型・`generate-seed.js` が現状を再現できない）は解消。

### 2026-07-26 — Claude

- 詳細資料側の記述を本番の実態に合わせて更新（コード変更なし、文書のみ）。
  - 「本番DBの状態」を実測値に差し替え（`quizzes` 5→6件 / `items` 160→299問、履歴は0件）。
  - 7章を「未完了の作業」から「投入の経緯と残課題」に改題。CSV139問と `todofuken-chizu` は投入済みのため完了として記載し、代わりに残課題（CSV投入手順が非定型・`generate-seed.js` が現状を再現できない）を明記。
  - ファイル構成の「★未完了」「★未投入」マーカーと、直近コミット一覧を最新化。
- 本番D1へ `SELECT` で実測して確認。書き込みは行っていない。

### 2026-07-26 — Codex

- `HANDOFF_FOR_CODEX.md` を全エージェント共通の引き継ぎ資料としてGit管理に追加。
- トップのネスト型習熟度一覧、色階級の説明コラム、直近のデプロイ状態を反映。undefined

## 既存の詳細資料（2026-07-26 時点）

家庭教師が生徒1名のために運用している学習クイズアプリ。この資料だけで現状を把握して作業に入れることを目指して書いている。

---

## 1. 全体像

- **用途**: 中学社会の一問一答クイズ。プレイヤーは生徒1名のみ。先生(=リポジトリ所有者)は統計閲覧と問題登録を行う。
- **本番URL**: `https://chishiki-quiz.pages.dev/`
- **リポジトリ**: `hasikure/knowledge_social_sciance`(mainブランチにpushするとCloudflare Pagesが自動デプロイ)
- **スタック**: Cloudflare Pages + Pages Functions + D1(SQLite互換)。ビルド工程はなく、静的HTML/CSS/JSをそのまま配信する。フレームワーク・npm依存はゼロ。

### 認証

`functions/_middleware.js` による自作認証。OAuthやCloudflare Accessは使っていない。

- 生徒/先生の2アカウント。ユーザー名・パスワードは環境変数(Cloudflare Pagesのsecret)で管理。
- ログイン成功でHMAC署名した `app_session` Cookie(HttpOnly, 30日)を発行。`role` クレームに `student` / `teacher` が入る。
- **全ページ・全APIがログイン必須**。未ログイン時はページなら `/login` へ302、APIなら401。
- Cookieが HttpOnly なのでJSから role を読めない。フロントは `/api/me` で自分のroleを取得する。

**重要**: 本番のパスワード類(`STUDENT_PASS` / `TEACHER_PASS` / `SESSION_SECRET`)はリポジトリ所有者本人が `wrangler pages secret put` で設定・管理している。**AIエージェント側はこの値を知らないし、知る必要もない。本番の認証後動作確認は所有者に依頼すること。** ローカルテストには後述の `.dev.vars` のテスト用資格情報を使う。

---

## 2. データベース

### スキーマ (`schema.sql`)

```sql
CREATE TABLE quizzes (          -- クイズマスタ
  id TEXT PRIMARY KEY,          -- 'sekai-isan' | 'rekishi' | ...
  name TEXT NOT NULL,           -- 表示名
  genre TEXT NOT NULL,          -- 教科。'syakai' | 'rika'
  section TEXT NOT NULL,        -- 分野。'地理' | '歴史' | '生物' など
  url TEXT NOT NULL,            -- サイトルート起点のパス 例: 'syakai/chikei/'
  max_score INTEGER NOT NULL DEFAULT 10,  -- 1ラウンドの満点(=出題数)
  sort_order INTEGER NOT NULL DEFAULT 0,  -- 表示順。分野の並び順もこれで決まる
  is_archived INTEGER NOT NULL DEFAULT 0  -- 1なら導線から外す(データは残す)
);

CREATE TABLE items (            -- 出題項目マスタ(全クイズ共通)
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  quiz_id TEXT NOT NULL REFERENCES quizzes(id),
  item_key TEXT NOT NULL,       -- クイズ内で一意な識別子
  label TEXT NOT NULL,          -- 表示名(一問一答クイズでは問題文そのものを入れることもある)
  answer TEXT NOT NULL,         -- 正解文字列
  category TEXT,                -- 用途はクイズごとに異なる(後述)
  extra_json TEXT,              -- クイズ固有情報のJSON文字列(hint, prefectures等)
  UNIQUE(quiz_id, item_key)
);

CREATE TABLE rounds (           -- 1プレイ分のサマリ
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  quiz_id TEXT NOT NULL,
  scope TEXT NOT NULL DEFAULT 'all',
  score INTEGER NOT NULL,
  total INTEGER NOT NULL,
  played_at TEXT DEFAULT (datetime('now')),
  role TEXT                     -- 'student' | 'teacher'。NULL は列追加前＝生徒扱い
);

CREATE TABLE attempts (         -- 解答履歴(1問1レコード)
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  item_id INTEGER NOT NULL REFERENCES items(id),
  round_id INTEGER NOT NULL REFERENCES rounds(id),
  is_correct INTEGER NOT NULL,
  answered_at TEXT DEFAULT (datetime('now'))
);
```

### 設計上の約束ごと(踏まないと壊れる箇所)

- **ユーザー識別カラムを持たない**。生徒が1人しかいない前提。複数生徒対応は現時点で要件外。
- **5段階習熟度は保存しない**。`attempts` の同一 `item_id` の直近2件を `ROW_NUMBER() OVER (PARTITION BY item_id ORDER BY answered_at DESC)` で都度算出する(未出題/1回正解/2回連続正解/1回間違い/2回連続間違い)。冗長な状態を持たない方針。ロジックは `functions/_lib/mastery.js`。
- **`category` の意味はクイズごとに違う**。地形なら「山地・山脈」等の分類、世界遺産なら「文化遺産/自然遺産」、地図クイズなら地方区分(スコープ絞り込みに使う)。汎用カラムとして扱うこと。
- **復習(即時フォローアップ)はDBに一切書き込まない**。詳細は次章。

### 本番DBの状態(2026-07-26 時点で確認)

`quizzes` 6件 / `items` 299件。うち現役(生徒がプレイできる)は4クイズ205問。プレイ履歴は `rounds` 0件・`attempts` 0件(まっさらな状態)。

| quiz_id | 表示名 | items | is_archived |
|---|---|---|---|
| `nihon-chiri` | 日本地理 | 107 | 0 |
| `todofuken-chizu` | 都道府県(地図) | 47 | 0 |
| `sekai-isan` | 日本の世界遺産 | 26 | 0 |
| `chikei` | 日本の地形 | 25 | 0 |
| `todofuken` | 都道府県 | 47 | 1 |
| `kencho` | 県庁所在地 | 47 | 1 |

`genre` は現状すべて `syakai`。

### マイグレーション運用

- `schema.sql` + `seed.sql` は**空のDBを初期化するためのもの**。既にデータが入っているDBに流すとUNIQUE制約違反で落ちる。
- 既存DBへの変更は `migrations/` にファイルを追加して適用する。既存の連番は `0001`〜`0005`(いずれも適用済み。ファイル名は migrations/ を参照)。冪等にするため全INSERTを `INSERT OR IGNORE` で書く方針。
- `seed.sql` は手で書かず **`node scripts/generate-seed.js > seed.sql` で生成する**。元データは `data/` 配下のCSV(8章参照)。`seed.sql` を直接編集しても次の生成で消える。

---

## 3. 出題ロジック

### 通常ラウンド

- 1ラウンド **固定10問**(`quizzes.max_score`)。
- 出題は **重み付きランダム抽選(Efraimidis-Spirakis法: `key = random()^(1/weight)` の上位N件)**。習熟度が低い項目ほど weight が大きく、選ばれやすい。実装は `assets/quiz.js` の `weightedSample()`、weightの値は `functions/_lib/mastery.js` の `SELECTION_WEIGHT`。
- 「全項目を毎回出す」方式ではない点に注意。

### 即時復習フォローアップ

ラウンド終了直後に「間違えた N 問だけもう一度」ボタンが出る。これは**純粋な練習であり、スコアにも習熟度にも一切影響しない**。`rounds` にも `attempts` にも書き込まない(`reviewMode` フラグでPOSTを丸ごとスキップ)。この仕様は所有者が明示的に決めたもので、変えないこと。

### 解答形式

現在はすべて**テキスト入力の一問一答**。以前は4択だったが、「地形の分類を答えさせる問題は名前に答えが入っている(例: 奥羽山脈→山地・山脈)」等の理由で廃止し、入力式に統一した。

正誤判定は `assets/quiz.js` の `isAnswerCorrect()`。`normalizeAnswer()` を通したうえで、**受け付ける表記のどれかと一致すれば正解**とする。記述式なので、書き方の違いで落とさないことを優先している。

- 問題タイプが `accept`(配列)を返していればその表記すべてを正解にする(例: 年代問題の「1192」「1192年」)。
- 指定が無い場合、正解が「黒潮(日本海流)」のように括弧で別名を併記していれば、全体・括弧の前・括弧の中のいずれでも正解にする。
- `normalizeAnswer()` が吸収するもの: カタカナ⇄ひらがな、全角英数⇄半角、空白、長音記号・中黒・カッコ。
- 接尾辞「県府都市区町村」は双方向で省略可（正解「青森県」に「青森」、正解「青森」に「青森県」）。
- 項目ごとの別解は `data/items_*.csv` の `accept` 列に `|` 区切りで書く（例: `リアス式海岸|リアス`）。`extra_json.accept` として保存される。
- 漢字⇄ひらがなの変換はしていない。必要なら `accept` に列挙する。

### 問題文はDBにある

**`items.label` が問題文そのもの**。1行=1問で、生成ロジックは無い。

```
label   次の説明にあてはまる地形は？
        「東北地方を南北に走る、日本で最も長い山脈」
answer  奥羽山脈
accept  (別解があれば)
```

同じ事物を別の角度から問いたい場合は**行を分ける**（歴史の「十七条の憲法」は語句・年代・人物の3行）。
`item_key` に `-year` のような接尾辞を付けて区別する。

図が要るクイズだけ `assets/questions/<quiz_id>.js` に描画関数を登録する。
`item` を受け取って表示用のDOM要素を返すだけで、問題文には関与しない。
図が作れない画面（先生用の一覧など）では `null` を返す。

```js
ChishikiQuestions.register("chizu-kigou", (item) => 記号のSVGを入れたdiv);
```
---

## 4. レベル / スコアの計算

`functions/api/dashboard.js` に実装。

### 経験値とレベル

正答率ではなく**これまで解いた問題数の累積**でレベルが上がる。

- 正解 = 10XP、不正解 = 3XP(即時復習はDB未記録なので加算されない)
- レベル到達に必要な累積XP: `a*x + b*x^2 + c*x^3`(x = level-1)
  - `a = 22.265962638928332`, `b = 2.737369622541412`, `c = -0.003332261469693653`
  - 上限 Lv100
- この係数は「Lv2到達=25XP(序盤は軽く)」「Lv80到達=約2000問」「Lv100到達=約3000問」の3点を満たすよう連立方程式を解いて決めたもの。カーブを変えたい場合は同じ方法で解き直すこと。

### 総合スコア

`totalScore` = 有効クイズごとのベストラウンドscoreの合計、`totalMax` = 有効クイズの `max_score` の合計。

以前は分母に「全項目数」を使っていたため、10問ラウンドで満点を取っても最大39%にしかならないバグがあった。**分子と分母のスケールを揃えること**(ラウンド単位 vs ラウンド単位)。

### 段位(色)

`assets/tier.js` が達成率%を8段階の色(灰→赤)に変換する。AtCoder風だが**他人との比較ではなく本人の到達度**を示すもの。

---

## 5. ファイル構成

```
/                         トップ(ダッシュボード)
  index.html              レベル・連続日数・総合スコア・クイズ一覧・週次進捗
  genres/index.html       ジャンル別の習熟度%と色の凡例
  login                   (functions/_middleware.js が生成)

  syakai/index.html       社会クイズ一覧 (/api/quizzes からDB駆動で生成)
  syakai/sekai-isan/      世界遺産クイズ(3パターン: 説明→名称 / 名称→所在県 / 名称→文化or自然)
  syakai/chikei/          地形クイズ(1パターン: 説明→名称)
  syakai/nihon-chiri/     日本地理クイズ(1パターン: 問題文→答え)
  syakai/todofuken-chizu/ 都道府県(地図)クイズ(地図を見て県名を入力)
  syakai/chizu-kigou/     地図記号クイズ(記号を見て名称を入力。画像問題の雛形)
  syakai/rekishi/         歴史クイズ(説明→語句 / 出来事→年代 / 業績→人物の3パターン)
  rika/index.html         理科(プレースホルダ)

  archive/todofuken/      旧・都道府県クイズ(地図クリック式)
  archive/kencho/         旧・県庁所在地クイズ(地図クリック式)
                          → 導線からは外したがURL直打ちで遊べる。DBでは is_archived=1

  teacher/index.html          先生用メニュー
  teacher/stats/index.html    統計・履歴(全体正答率/クイズ別/苦手トップ10/直近20件)
  teacher/items/index.html    問題一覧(クイズ別。地図記号は記号の絵付き)
  teacher/questions/index.html 問題登録フォーム

  assets/quiz.js              クイズエンジン(入力式・重み付き抽選・即時復習・正誤判定)
  assets/questions.js         出題定義のレジストリ
  assets/questions/*.js       クイズごとの出題定義(出題ページと問題一覧が共用)
  assets/prefecture-map-quiz.js 旧地図クリック式エンジン(archive/ が使用)
  assets/chizu-kigou-symbols.js 地図記号のSVG定義(クイズ画面と問題一覧で共用)
  assets/tier.js              達成率→色
  assets/style.css

  functions/_middleware.js    認証ゲート
  functions/_lib/mastery.js   習熟度判定・重み・ジャンルスコア(アンダースコア始まりなのでルーティングされない)
  functions/api/*.js          後述

  schema.sql                  空DB初期化用
  seed.sql                    空DB初期化用(自動生成、直接編集しない)
  scripts/generate-seed.js    CSV -> seed.sql の変換(検証も行う)
  migrations/                 既存DBへの差分適用
  wrangler.toml               Pages設定 + D1バインディング(binding名 DB)
  data/                       問題データの正本CSV(8章参照)
```

## 6. API

すべて `functions/api/` 配下。ログイン必須。

| エンドポイント | 内容 |
|---|---|
| `GET /api/me` | ログイン中の role を返す(Cookieが HttpOnly のため必要) |
| `GET /api/quizzes` | クイズ一覧。`?include_archived=true` は**先生のみ**有効(生徒が付けても無視される) |
| `GET /api/items?quiz_id=&scope=` | 項目一覧。習熟度(`mastery`)と抽選用の重み(`weight`)を計算して返す |
| `POST /api/items` | 問題の新規登録。**先生のみ**(それ以外は403) |
| `GET /api/rounds?quiz_id=&scope=` | 自己ベストの `{score, total}` |
| `POST /api/rounds` | ラウンド結果を記録。body は `{quiz_id, scope, attempts: [{item_id, is_correct}]}`。**`is_correct` は boolean**(数値0/1を渡すと400) |
| `GET /api/dashboard` | トップ用の集計(ベストスコア・総合スコア・XP/レベル・連続日数・週間回数・ジャンル別%) |
| `GET /api/stats` | 統計・履歴。**先生のみ**(それ以外は403) |

---

## 7. 新しいクイズを追加する手順

画面の階層は **教科(`genre`) → 分野(`section`) → クイズ**。分野分けも並び順もDBが決めるので、
コードを触らずにクイズを増やせる。`syakai/rekishi/` を写して作るのが早い。

1. **`data/quizzes.csv` に1行足す**

   ```csv
   id,name,genre,section,url,max_score,sort_order,is_archived
   seibutsu-shokubutsu,植物のつくり,rika,生物,rika/seibutsu-shokubutsu/,10,300,0
   ```

   - `sort_order` は10刻み。分野の並び順は「その分野の最初のクイズの `sort_order`」で決まるので、
     教科ごとに帯を分けておくとよい(例: 社会=10〜199、理科の物理=200番台/化学=300番台/生物=400番台/地学=500番台)。
   - `genre` を新設した場合は `index.html` の `SUBJECT_LABELS` に表示名を足す(ここだけは今もコード側)。

2. **`data/items_<quiz_id>.csv` を作る**。列は `quiz_id,item_key,label,answer,category,accept,extra_json`。**1行=1問**で、`label` に問題文をそのまま書く。

   - `category` は範囲の絞り込みに使う想定の分類(歴史なら時代、地形なら山地・平野など)。
   - 同じ事物を複数の角度から問うなら行を分け、`item_key` に `-year` のような接尾辞を付ける。
   - `accept` は別解を `|` 区切りで。カナ⇄かな・全角半角・接尾辞(県/市など)・長音や中黒はエンジンが吸収するので書かなくてよい。
   - `extra_json` は補足メモ用。出題には使わない(問題文は `label` が全て)。

3. **図が要る場合だけ** `assets/questions/<quiz_id>.js` に描画関数を登録する。文字だけのクイズはこの手順は不要。

   - `assets/questions/chizu-kigou.js` が雛形。図が作れない画面では `null` を返すこと。

4. **出題ページ `<url>/index.html` を作る**。`questions.js` → 出題定義 → `quiz.js` の順に読み込み、`questionTypes: ChishikiQuestions.get("<quiz_id>")` を渡すだけ。`syakai/chikei/index.html`(32行)が最小の例。
   - 先生用の一覧 `teacher/items/index.html` にも読み込みタグを足すこと。

5. **`node scripts/generate-seed.js > seed.sql`**。ここで必須列・未知の`quiz_id`・`item_key`重複・`extra_json`のJSON妥当性が検査される。

6. **差分を `migrations/000N_*.sql` に書いて本番に適用**。`seed.sql` は空DB専用なので本番に流さないこと。
   `INSERT OR IGNORE` で書けば再実行しても安全になる。

7. **ローカルで確認** → commit → push(pushで自動デプロイ)。

> 既存クイズと共有している `assets/quiz.js` を変更した場合は、**全クイズの回帰確認が必要**。

---

## 8. 問題データの管理(CSV一本化)

### 7-1. 正本は `data/` 配下のCSV

問題データもクイズ定義も**すべてCSVが正本**。JS内のハードコードは廃止した。Excel等でそのまま開ける。

| ファイル | 内容 |
|---|---|
| `data/quizzes.csv` | クイズ定義。列は `id,name,genre,url,max_score,is_archived` |
| `data/items_<quiz_id>.csv` | そのクイズの問題。列は `quiz_id,item_key,label,answer,category,extra_json` |

現在は `items_sekai-isan`(26) / `items_chikei`(25) / `items_nihon-chiri`(107) / `items_todofuken-chizu`(47) / `items_todofuken`(47) / `items_kencho`(47) の6ファイル、計299問。

`nihon-chiri` の中身は `category` で系統が分かれている: `basic`(15) / `todofuken` 都道府県の説明文→県名(47) / `tokusanhin` 特産品→都道府県(36) / `nousuisan` ランキング空欄補充(9)。

### 7-2. 問題を追加・修正する手順

1. 該当CSVを編集する(新しいクイズを足すなら `data/quizzes.csv` に1行足し、`data/items_<id>.csv` を作る)。
2. `node scripts/generate-seed.js > seed.sql` で再生成する。このときスクリプトが検証も行う。
   - 必須列(`quiz_id` / `item_key` / `label` / `answer`)の空チェック
   - `data/quizzes.csv` に無い `quiz_id` の検出
   - 同一クイズ内での `item_key` 重複の検出
   - `extra_json` がJSONとして妥当か
   - 問題があれば `ファイル名:行番号` 付きでエラーになる
3. **既存の本番DBには `seed.sql` を流さない**(UNIQUE制約違反になる)。差分だけを `migrations/000N_*.sql` に書いて適用する。`seed.sql` は空DBを作り直すとき専用。

CSVは `parseCsv()` がRFC4180準拠で読むため、セル内のカンマ・改行・二重引用符をそのまま書ける(ランキング問題は実際に複数行の問題文を1セルに入れている)。

### 7-3. 都道府県(地図)クイズ

「地図で色がついている県はどこか」をテキスト入力で答えるクイズ。`archive/todofuken/` のクリック式を置き換えるもの。

- `syakai/todofuken-chizu/index.html` — 地方ごとにズームしたSVG地図を `<template>` に持ち、出題ごとに複製して該当県だけ着色する。
- `assets/quiz.js` — `renderQuestion()` が `q.visual`(DOM要素)を表示できるよう拡張済み。**共有エンジンなので、ここを触ったら他の全クイズの回帰確認が必要。**

### 7-4. 残っている課題

- **内容の裏取り**(作成者による申し送り):
  - 農水産ランキングの数値は二次情報源から取得。一次資料(e-Stat等)での確認が望ましい。
  - 特産品の「黒糖」は沖縄以外(鹿児島の奄美群島など)でも生産されるため、正誤判定で割れる可能性がある。
- 教科の分野分けがトップの `SECTION_BY_QUIZ` によるハードコードのまま。歴史・公民・理科を足すときは、`quizzes` テーブルに分野列を持たせて暫定マッピングを廃止する。
- `teacher/questions/` から登録した問題はDBに直接入るため、CSVには反映されない。CSVとDBがずれるので、フォームから足したら同じ内容をCSVにも追記すること(将来的にはCSVエクスポート機能があると望ましい)。

---

## 9. ローカル開発

```bash
# 依存インストールは不要(npxで都度取得)

# 空のローカルD1を初期化
npx wrangler d1 execute chishiki-quiz-db --local --file=./schema.sql
npx wrangler d1 execute chishiki-quiz-db --local --file=./seed.sql

# 開発サーバ(.dev.vars を自動で読む)
npx wrangler pages dev . --port 8788
```

`http://localhost:8788/` を開き、テスト用資格情報でログインする。これらは `.dev.vars`(gitignore済み・ローカル専用)に定義されている。

- 生徒: `teststudent` / `testpass123`
- 先生: `testteacher` / `testpass456`

**ローカルD1は `.wrangler/` 配下のSQLiteファイルで、本番とは完全に別物。**テストプレイの記録が残るので、まっさらにしたい場合は `.wrangler/` を消して上記を再実行する。

### 本番DBを直接見る

```bash
npx wrangler d1 execute chishiki-quiz-db --remote --command="SELECT ..."
```

---

## 10. 環境の癖(ハマりどころ)

Windows + Git Bash 環境。以下は実際に踏んだもの。

- **`curl -w '%{http_code}'` が実際は成功しているのに `000` を返すことがある**(schannelのSSL再ネゴシエーション絡み)。ステータス確認は `curl -s -i ... | grep -m1 '^HTTP/'` でヘッダーを直接読むこと。
- **`node -e` に `/c/Users/...` 形式のパスを渡すと解決できない**。`C:/Users/...` 形式を使う。
- **wranglerを複数プロセスで同時に走らせるとnpxキャッシュとD1ファイルがロックされ `EBUSY` になる**。別のエージェントやdevサーバが動いていないか確認する。バージョンを固定(`npx wrangler@4.113.0`)すると自動アップグレードによる衝突を避けられる。
- ローカルdevサーバの停止は `netstat -ano | grep :8788` → `taskkill //PID <pid> //F`。

---

## 11. 直近のコミット

```
50f1493 Rename shared handoff document
c15397f Add shared agent handoff protocol
3617fd0 Organize dashboard mastery overview
1dc5b25 feat: add map quiz features and new geography items
e9af0b6 Move quiz metadata into the database and switch to typed answers
596dd63 Switch level to cumulative XP from questions answered
9c805e9 Add teacher-only stats/history and question-registration pages
```

`e9af0b6` で「クイズ情報をコードのハードコードからDB(`quizzes`テーブル)へ移す」「4択→入力式」「地図クイズをarchive送り」を行い、対応するmigration `0001` は本番適用済み。`1dc5b25` で都道府県(地図)クイズと新コンテンツを追加し、その後DB側の投入も完了している(8章参照)。
